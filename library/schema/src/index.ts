export type {
  SchemaInterface,
  SchemaInterfaceIdentity,
  SchemaInterfaceType,
} from "./interface";

export type {
  Schema,
  SchemaPrototype,
  Property,
  PropertyPrototype,
} from "./schema";

export { expandSchema, getSchemaProperties } from "./utils";
